[
  {
    "name": "Check price empty watchlist",
    "steps": [
      {
        "send": { "callback": "price:all" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Your watchlist is empty — tap Add to get started." }
          }
        ]
      }
    ]
  }
]
